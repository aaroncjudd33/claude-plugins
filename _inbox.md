# Inbox — ajudd-claude-plugins  (SUPERSEDED — see below)

> **This file is no longer the inbox.** As of the item-driven-sessions overhaul,
> the canonical inbox lives in the local session tier:
>
>     ~/.claude/memory/sessions/ajudd-claude-plugins/_inbox.md
>
> That is the file `/session:start` reads and `pick`s from. This repo-root file is
> kept only as history (git log preserves the items that passed through it). Do not
> add new items here — add them to the local inbox via `/session:start` → `new <desc>`,
> or `/session:inbox`.
>
> Items that lived here have all been resolved:
> - Post-Deploy Validation Checklist — shipped
> - session:migrate user-filter + Playwright Phase C — shipped
> - CDK/DynamoDB finish-gate — shipped (session v1.49.6)
> - Item-driven sessions for plugin & personal work — in-flight (this overhaul)
